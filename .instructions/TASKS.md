# Tasks To Complete

1. Please lock the uploading of images for users that have not purchased a subscription, do not do anything regarding the subscription just add a boolean column to users and default it to false. Change both the backend and frontend of all image uploads so that when this value is false, the user is not able to upload any images.

2. Add a favicon that suits the theme of the application.

3. Change all of the title tags so that they better display what is currently being shown on the page, include the application name "Universe Tracker".

4. Write tests to ensure all new functionality works as intended.